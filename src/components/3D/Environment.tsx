import React from 'react';

const Environment: React.FC = () => {
  // Minimalist background, letting lights do the work.
  return null;
};


Environment.displayName = 'Environment';

export default React.memo(Environment);
